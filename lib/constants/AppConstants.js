'use strict';

const UserEvents = {
  CONVERT: 'CONVERT',
  EDIT: 'EDIT'
};

const StoreEvents = {
  CHANGE: 'CHANGE'
};

const ErrorEvents = {
  API_EEROR: 'API_ERROR',
  VALIDATION_ERROR: 'VALIDATION_ERROR'
}

export {UserEvents, StoreEvents, ErrorEvents};
